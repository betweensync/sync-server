# AWS Key
aws.access.key=${filtering.aws.access.key}
aws.secret.key=${filtering.aws.secret.key}

jms.user.name=${filtering.jms.user.name}
jms.password=${filtering.jms.password}
jms.broker.url=${filtering.jms.broker.url}
jms.session.cache.size=${filtering.jms.session.cache.size}
jms.queue.name=${filtering.jms.queue.name}
jms.listener.container.concurrency=${filtering.jms.listener.container.concurrency}


# Cloudant URL
cloudant.url=${filtering.cloudant.url}
cloudant.username=${filtering.cloudant.username}
cloudant.password=${filtering.cloudant.password}

# Web Socket 
websocket.port=${filtering.websocket.port}

# Temp file directory upload and download
temp.dir=${filtering.temp.dir}